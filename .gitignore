# -----------------------
# OS / Editor / IDE
# -----------------------
# macOS
.DS_Store

# Windows thumbnail cache
Thumbs.db

# IDEs
.vscode/
.idea/

# shell / history
.history

# Jupyter
.ipynb_checkpoints/

# -----------------------
# Terraform
# -----------------------
# state files & folders
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.*

# -----------------------
# Python
# -----------------------
# virtual envs
.venv/
venv/

# bytecode / cache
__pycache__/
*.pyc
*.pyo
*.pyd

# pip/uv/poetry lock
# catatan: biasanya commit lock file; hapus/komentari baris ini jika kamu ingin COMMIT uv.lock
uv.lock          

# -----------------------
# Environment / Secrets
# -----------------------
# dotenv files (may contain secrets)
.env
# environment files (contains secrets)
.env.*           


# generic json credentials (IGNORE all .json — exceptions below)
*.json
!package.json
!package-lock.json

# specific credential files (KEEP THESE as-is because you said penting)
**google_credentials.json
playground/dbt_poc_encryption/profiles.yml
/home/fahernkhan/Documents/data_engineer/data-engineering-zoomcamp/playground/dbt_poc_encryption/profiles.yml

# NOTE: jangan hapus atau ubah baris credential di atas tanpa sengaja

# -----------------------
# Logs
# -----------------------
logs/
*.log

# -----------------------
# Data files / DB
# -----------------------
# ignore datasets and common output formats
data/
*.parquet
*.csv
<<<<<<< HEAD
.duckdb

# exception: keep this specific file even though we ignore data/ (negation must come AFTER data/)
!week_6_stream_processing/avro_example/data/rides.csv

# other project-specific data folders (keep pattern as provided)
**/ny_taxi_postgres_data/*

# -----------------------
# Spark / Big Data artifacts
# -----------------------
# spark binary / downloads
spark-*.tgz
spark/
spark-warehouse/

# spark output (partitioned parquet, etc)
yellow_2025_11_repart4/

# -----------------------
# DBT
# -----------------------
target/
dbt_packages/
recovered_files/
playground/dbt_poc_encryption/profiles.yml   # (already listed above; kept for visibility)

# -----------------------
# Misc / custom
# -----------------------
serving_dir

# -----------------------
# Final notes (comments only)
# -----------------------
# - Aku mempertahankan semua baris credential yang kamu masukkan (google_credentials.json, profiles.yml, dan path absolut).
# - Jika ingin mengecualikan file .json tertentu dari ignore, tambahkan negation (!) di bawah pola *.json.
# - Kalau mau commit uv.lock, hapus atau komentari baris "uv.lock" di atas sebelum commit.
=======
*.duckdb
>>>>>>> 69ac907 (Add .duckdb to .gitignore)
